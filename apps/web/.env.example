# Frontend environment — copy to .env.local and adjust per environment.

# Python (FastAPI) backend base URL — see apps/py-api/
VITE_PY_API_URL=http://localhost:8000

# Kotlin (Ktor 3) backend base URL — see apps/kt-api/
VITE_KT_API_URL=http://localhost:8080
