# Chapter 05 — Optimization — objectives, bounds, callbacks

Stub — covers knapsack + callbacks: objectives, bounds, enumeration.

See [`../../../docs/plan.md`](../../../docs/plan.md) chapter 05 for scope.
Per-chapter teaching notes will land at `docs/chapters/05-*.md` when the chapter is written.

## Run (once implemented)

```bash
uv run python -m py_cp_sat_ch05
```

Currently raises `NotImplementedError`.
