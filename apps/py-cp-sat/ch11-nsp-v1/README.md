# Chapter 11 — NSP v1 — toy instance, hard constraints only

Stub — covers toy 5-nurse x 14-day NSP, every textbook hard constraint.

See [`../../../docs/plan.md`](../../../docs/plan.md) chapter 11 for scope.
Per-chapter teaching notes will land at `docs/chapters/11-*.md` when the chapter is written.

## Run (once implemented)

```bash
uv run python -m py_cp_sat_ch11
```

Currently raises `NotImplementedError`.
