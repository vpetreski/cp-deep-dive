# Chapter 13 — NSP v3 — INRC-II benchmark-scale + tuning

Stub — covers INRC-II instance, hints, solver tuning, LNS.

See [`../../../docs/plan.md`](../../../docs/plan.md) chapter 13 for scope.
Per-chapter teaching notes will land at `docs/chapters/13-*.md` when the chapter is written.

## Run (once implemented)

```bash
uv run python -m py_cp_sat_ch13
```

Currently raises `NotImplementedError`.
