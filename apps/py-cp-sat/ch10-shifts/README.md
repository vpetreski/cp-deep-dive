# Chapter 10 — Time, shifts, transitions

Stub — covers Boolean shift grid + AddAutomaton for transition rules.

See [`../../../docs/plan.md`](../../../docs/plan.md) chapter 10 for scope.
Per-chapter teaching notes will land at `docs/chapters/10-*.md` when the chapter is written.

## Run (once implemented)

```bash
uv run python -m py_cp_sat_ch10
```

Currently raises `NotImplementedError`.
