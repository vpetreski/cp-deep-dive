# Chapter 12 — NSP v2 — soft constraints, preferences, fairness

Stub — covers NSP + preferences + fairness + lexicographic objectives.

See [`../../../docs/plan.md`](../../../docs/plan.md) chapter 12 for scope.
Per-chapter teaching notes will land at `docs/chapters/12-*.md` when the chapter is written.

## Run (once implemented)

```bash
uv run python -m py_cp_sat_ch12
```

Currently raises `NotImplementedError`.
