# Chapter 06 — The global-constraints tour — Circuit, Table, Automaton, Element

Stub — covers Circuit, Table, Automaton, Element, Inverse, LexLeq, Reservoir.

See [`../../../docs/plan.md`](../../../docs/plan.md) chapter 06 for scope.
Per-chapter teaching notes will land at `docs/chapters/06-*.md` when the chapter is written.

## Run (once implemented)

```bash
uv run python -m py_cp_sat_ch06
```

Currently raises `NotImplementedError`.
