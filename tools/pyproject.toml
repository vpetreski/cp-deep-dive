[project]
name = "cp-deep-dive-tools"
version = "0.1.0"
description = "Standalone tools for cp-deep-dive (schedule validator, instance loaders, etc)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.23",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
]
